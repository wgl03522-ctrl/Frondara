{
  "name": "@pnode/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "pnode": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pnode/core": "*",
    "@pnode/storage": "*",
    "commander": "latest"
  },
  "devDependencies": {
    "typescript": "latest",
    "vitest": "latest"
  }
}
